import React from 'react';

const useroutput = (props) => {
    return (
        <div>
            <p>{props.username}</p>
            <p>Paragraph 2</p>
        </div>
    )
};

export default useroutput;